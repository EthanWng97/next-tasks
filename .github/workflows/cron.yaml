name: 15-minute-cron
on:
  schedule:
    - cron: '30 16 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request GET \
          --url 'https://next-scripts-git-master-navepnow.vercel.app/api/leetcode-en'
